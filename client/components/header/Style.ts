import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles(() => ({
    container: {
        width: '100%',
        height: 50
        // backgroundColor: ''
    }
}));

export default useStyle;
